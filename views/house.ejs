<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous"> -->
        <link href="lib/noty.css" rel="stylesheet" />
        <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>

        <!-- <script src="lib/noty.js" type="text/javascript"></script> -->
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />

        <title>Food Ordering Website</title>
        <link rel="stylesheet" href="house.css" />
        <!-- Include jQuery from a CDN -->
    </head>
    <body>
        <%- include('./components/header') %> <% if(locals.messege &&
        locals.messege.length){ %>
        <div class="pop-up-container" id="popupContainer">
            <div class="pop-up-content"><%= messege %></div>
        </div>

        <%}%> <%- include('./components/left-section') %> <% if(User){ %>
        <%-include('./components/right-section') %><% }%>
        <%-include('./components/main') %>
        <style>
            #notification-box {
                position: fixed;
                bottom: 0px;
                right: 0px;
                width: 300px;
                backdrop-filter: blur(3px);
                border-radius: 20px;
                max-height: 300px;
                overflow-y: auto;
                z-index: 20;
            }
        </style>
        <div id="notification-box"></div>

        <script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>
        <script src="notification.js"></script>
    </body>
    <script>
        // Automatically hide the pop-up after 3 seconds
        setTimeout(function () {
            var popupContainer = document.getElementById("popupContainer");
            if (popupContainer) {
                popupContainer.style.background =
                    popupContainer.innerText == "SIGN-OUT succesfully"
                        ? "rgb(220, 72, 72)"
                        : "rgb(124, 227, 124)";
                popupContainer.style.opacity = "0";
                setTimeout(function () {
                    popupContainer.style.display = "none";
                }, 4000); // Additional delay for animation completion
            }
        }, 500);
    </script>

    <!-- <script src="house.js"></script> -->
    <script type="module" src="item.js"></script>
    <script type="module" src="cart.js"></script>
    <script type="module" src="add-item.js"></script>
</html>
